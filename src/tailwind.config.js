/** @type {import('tailwindcss').Config} */
export default {
    content: [
      "./index.html", // Включает HTML-файл
      "./src/**/*.{js,ts,jsx,tsx}", // Включает все файлы в папке src с расширениями js, ts, jsx, tsx
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  };
  